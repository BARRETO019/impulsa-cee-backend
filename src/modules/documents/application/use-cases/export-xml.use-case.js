/**
 * Caso de uso: exportar XML
 * -------------------------
 * De momento dejamos el mismo comportamiento que tenías:
 * "XML no implementado"
 */
class ExportXMLUseCase {
  async execute() {
    return {
      message: "XML no implementado"
    };
  }
}

module.exports = ExportXMLUseCase;